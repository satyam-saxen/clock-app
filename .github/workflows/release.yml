name: Release Electron App

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-mac:
    runs-on: macos-latest
    strategy:
      matrix:
        arch: [x64, arm64]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - name: Package Electron app
        run: npx electron-packager . ClockScreensaver --platform=darwin --arch=${{ matrix.arch }} --icon=icon.icns --overwrite --ignore="ClockScreensaver-darwin-"
      - name: Ad-hoc sign app
        run: codesign --force --deep -s - ClockScreensaver-darwin-${{ matrix.arch }}/ClockScreensaver.app
      - name: Zip app
        run: ditto -c -k --sequesterRsrc --keepParent ClockScreensaver-darwin-${{ matrix.arch }}/ClockScreensaver.app ClockScreensaver-mac-${{ matrix.arch }}.zip
      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: ClockScreensaver-mac-${{ matrix.arch }}.zip

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - name: Package Windows widget
        run: npx electron-packager . ClockWidget --platform=win32 --arch=x64 --overwrite --ignore="ClockWidget-win32-"
      - name: Zip app
        run: Compress-Archive -Path ClockWidget-win32-x64\* -DestinationPath ClockWidget-win64.zip
      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: ClockWidget-win64.zip
